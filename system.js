'use strict';
 function a(){
    function log(str) {
        console.log(str);
        console.log(undefined);
    }
    var api = function check(api) {
    var name = 'asap';
    var i;
    if (i === undefined || i === 0){
            i = 1;
                while (i < 1000 && i !== undefined) {
                    i++;
                    log(i);
                }
            }
        };
}
